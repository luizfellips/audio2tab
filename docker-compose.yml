services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./.env:/app/.env:ro
      - ./app:/app/app
      - ./frontend:/app/frontend
      - ./samples:/app/samples
      - ./outputs:/app/outputs
    restart: unless-stopped
